let opera = '/';
let a = 5;
let b = 6;

switch(opera) {
    case '+':
        alert('a + b =' + (a + b)); break;
    case '-':
        alert('a - b =' + (a - b)); break;
    case '*':
        alert('a * b =' + (a * b)); break;
    case '/':
        alert('a / b =' + (a / b)); break;
    default:
        alert('khong co phep toan'); break;
}